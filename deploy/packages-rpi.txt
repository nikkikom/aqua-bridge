# Raspberry Pi OS Lite 32-bit (Trixie) — aqua-bridge
# sudo apt-get update
# sudo apt-get install -y $(grep -vE '^#|^$' deploy/packages-rpi.txt | xargs)
#
# Zero W: 32-bit only. linux-headers-rpi-v6 is the Zero / Pi 1 kernel.

build-essential
gcc
make
pkg-config
git
cmake
flex
bison
libssl-dev
bc
linux-headers-rpi-v6
i2c-tools
python3
python3-dev
python3-pip
python3-venv
python3-smbus
python3-spidev
python3-lgpio
libgpiod-dev
gpiod
minicom
lm-sensors
# HTTPS API: install-pi.sh creates a self-signed certificate
openssl

# USB / MQTT / MPC via apt so a venv --system-site-packages can import them.
# Do not pip-install numpy on Zero W.
python3-hid
python3-usb
python3-numpy
python3-yaml
python3-paho-mqtt
python3-aiohttp
liquidctl

# Tests on the Pi (pytest -m hardware, HYPOTHESIS_PROFILE=pi)
python3-pytest
python3-hypothesis
